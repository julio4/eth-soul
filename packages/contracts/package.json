{
  "name": "contracts",
  "license": "MIT",
  "version": "0.0.1",
  "scripts": {
    "clean": "pnpm hardhat clean",
    "compile": "pnpm hardhat compile",
    "test": "pnpm hardhat test",
    "dev": "pnpm run build && pnpm run deploy",
    "build": "pnpm clean && pnpm compile",
    "start": "pnpm run deploy",
    "types": "pnpm typechain --target=ethers-v5 './artifacts/contracts/**/+([a-zA-Z0-9_]).json' --out-dir './typechain-types'",
    "lint": "prettier . --check && pnpm eslint",
    "lint:fix": "prettier . --write && pnpm eslint --fix",
    "eslint": "eslint . --ext .js,.ts",
    "solhint": "solhint 'contracts/**/*.sol'",
    "deploy": "pnpm hardhat node --export './deployments/1337.json' --write true",
    "deploy:sepolia": "pnpm hardhat deploy --network sepolia --export './artifacts/contracts/Sel.sol/Sel.json' --write true",
    "verify:sepolia": "pnpm hardhat etherscan-verify --network sepolia",
    "deploy:scroll": "pnpm hardhat deploy --network scrollAlpha --export './artifacts/contracts/Sel.sol/Sel.json' --write true",
    "deploy:optimism": "pnpm hardhat deploy --network optimismGoerli --export './artifacts/contracts/Sel.sol/Sel.json' --write true"
  },
  "devDependencies": {
    "@ethersproject/abi": "^5.7.0",
    "@ethersproject/providers": "^5.7.2",
    "@nomicfoundation/hardhat-chai-matchers": "^1.0.4",
    "@nomicfoundation/hardhat-network-helpers": "^1.0.6",
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "@nomiclabs/hardhat-ethers": "npm:hardhat-deploy-ethers@0.3.0-beta.13",
    "@nomiclabs/hardhat-etherscan": "^3.1.7",
    "@openzeppelin/contracts": "^4.8.0",
    "@typechain/ethers-v5": "^10.1.1",
    "@typechain/hardhat": "^6.1.4",
    "@types/chai": "^4.3.4",
    "@types/mocha": "^9.1.1",
    "@types/node": "^16.18.3",
    "@typescript-eslint/eslint-plugin": "^5.43.0",
    "@typescript-eslint/parser": "^5.43.0",
    "chai": "^4.3.7",
    "dotenv": "^16.0.3",
    "eslint": "^8.27.0",
    "eslint-config-prettier": "^8.5.0",
    "ethers": "^5.7.2",
    "hardhat": "^2.12.2",
    "hardhat-deploy": "^0.11.20",
    "hardhat-gas-reporter": "^1.0.9",
    "solhint": "^3.3.7",
    "solidity-coverage": "^0.8.2",
    "ts-node": "^10.9.1",
    "typechain": "^8.1.1",
    "typescript": "^4.9.3"
  }
}
